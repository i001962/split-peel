# Banny Studio Templates

Reusable `.bs` packages live here.

Expected Proof Patrol starter template:

```text
templates/proof-patrol-base.bs
```

Create it in Banny Studio with Perry Provenance and Dr. Fauxnana staged in the default proof-lab scene, then use `examples/proof-patrol/studio-pipeline.json` as the first draft-only pipeline config.

